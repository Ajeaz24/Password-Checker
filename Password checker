#Password Checker
username = input("Write your Username ") 
password = input("Enter your Password ")
password_length = len(password) # Getting the input Password length 
hidden_password = ("*" * password_length) # Multiplying the Password length with * to mask the password
print(f"{username}, your password, {hidden_password} is {password_length} letters long")
